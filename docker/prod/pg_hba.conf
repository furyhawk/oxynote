# the embedded PostgreSQL's only access rule, handed to the server with
# hba_file on every start. It listens on a unix socket and on no TCP port,
# and every connection still needs a password: core dials the data sources
# workspace members configure, and one of them could name this socket.
local all all scram-sha-256
